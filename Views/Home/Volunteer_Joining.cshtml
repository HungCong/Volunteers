
@using Volunteers.Models.Entity;
@using Volunteers.Models.DTO;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Đợt tình nguyện đang tham gia";
    var LstVolunteer = ViewBag.LstVolunteer as List<RoundVolunteerDTO>;
    int dem = 0;
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Đợt tình nguyện bạn đang tham gia
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Trang chủ</a></li>
            <li class="active">Đợt TN đang tham gia</li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    
                    <div class="box-body">
                        <table id="example2" class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Địa điểm</th>
                                    <th>Bắt đầu ngày</th>
                                    <th>Kết thúc ngày</th>
                                    <th>Trạng thái</th>
                                    <th>Số tình nguyện viên</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in LstVolunteer)
                                {
                                    dem++;
                                    <tr>
                                        <td>@dem</td>
                                        <td>@item.Place</td>
                                        <td>@item.StartDate.Value.ToString("dd/MM/yyyy")</td>
                                        <td>@item.EndDate.Value.ToString("dd/MM/yyyy")</td>
                                        @if (item.Status == 2)
                                        {
                                            <td><span class="label label-primary">Mở đăng ký</span></td>
                                        }
                                        else if (item.Status == 3)
                                        {
                                            <td><span class="label label-default">Hết hạn đăng ký</span></td>
                                        }
                                        else if (item.Status == 4)
                                        {
                                            <td><span class="label label-info">Đã duyệt</span></td>
                                        }
                                        else if (item.Status == 5)
                                        {
                                            <td><span class="label label-danger">Đã kết thúc</span></td>
                                        }
                                        <td>
                                            <a href="/Home/ListVolunteer/@item.Round_Volunteer_ID" class="btn btn-primary" title="Danh sách tình nguyện tham gia">@item.Register Tình nguyện viên</a>
                                        </td>
                                        <td>
                                            <a href="/Home/ListMaterial_Support/@item.Round_Volunteer_ID" class="btn btn-default" title="Vật chất đợt tình nguyện"><i class="fa fa-info-circle"></i></a>
                                        </td>
                                    </tr>
                                }

                            </tbody>
                            <tfoot>
                                <tr>
                                    <th>#</th>
                                    <th>Địa điểm</th>
                                    <th>Bắt đầu ngày</th>
                                    <th>Kết thúc ngày</th>
                                    <th>Trạng thái</th>
                                    <th>Tình nguyện viên</th>
                                    <th></th>
                                </tr>
                            </tfoot>
                        </table>
                    </div><!-- /.box-body -->
                </div><!-- /.box -->

            </div><!-- /.col -->
        </div><!-- /.row -->
    </section><!-- /.content -->
</div><!-- /.content-wrapper -->



@section jsAdmin{

    <script>
      
    </script>

}

